-- Check if the user table has data after signup
SELECT * FROM users;

-- Check if the database is being dropped or recreated (should NOT be run on every restart)
-- DO NOT run DROP DATABASE or DROP TABLE on every server start.

-- If you want to see all databases
SHOW DATABASES;

-- If you want to see all tables in smartharvest
USE smartharvest;
SHOW TABLES;
